in order to run the name detector, run these first:

pip install -U pip setuptools wheelpip 
install -U spacy
python -m spacy download en_core_web_sm

(last line you may need to use python3 instead of python depending on what version you have on your machine)
